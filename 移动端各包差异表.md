# Capyroom 移动端各包差异表（9-23 起，发版前对一遍）

四种包**同一份代码、同一次提交打**；版本号取 `tauri.ios.conf.json` / `tauri.android.conf.json`（两处保持一致），build 号＝git 提交数。
下面是**允许不一样**的全部地方，此外任何差异都算 bug。

| | iOS 商店包（全球一个包） | 国内安卓官网包 | Play 包 | 测试包（iOS ad-hoc / 安卓 _t） |
|---|---|---|---|---|
| workflow | build-appstore | build-android `release=true` | build-android-play | build-adhoc / build-android `sign=true` |
| 服务端（account.js WEB_BASE） | 国内 www.tybbtech.com | 国内 | 海外 stampday（CI 替换） | 国内 |
| 登录 | 手机号 + Apple | 手机号 | Google | 同所在平台 |
| 商店 | StoreKit：只有主题包（调汤里带锁的主题）+ 恢复购买（9-25 起物件/手拭巾不卖；老单件恢复购买仍认） | 无（没有任何购买入口、没有恢复购买） | 无（Play 结算未接） | iOS 有沙盒商店 |
| 付费主题 | 带锁可买 | **不显示**（不是免费） | **不显示** | 显示（看效果） |
| 内测「全部解锁」 | 无 | 无 | 无 | 有（CAPY_INTERNAL） |
| 备案号（调汤页底） | 显示 | 显示 | 不显示（IS_OVERSEAS） | 显示 |
| 下载位置 | App Store（含中国大陆，9-23 开） | www.tybbtech.com/capyroom/dl/capyroom.apk | Play | _t/install.html、_t/android.html |

**内容一律相同**：手拭巾和庭院小物按四条线解锁（专注 / 休息 / 来访 / 长专注，见《商业化方案说明书v3》），所有包都一样，**哪个包都买不到**；只有主题包收费。
同一账号（国内服务端）在 iOS 买的，安卓登录后照样拥有。

## 规矩
- 付费只有主题包：在 `rewards_catalog.json` 的 themes 里标 `paid`/`sku`，没商店的包自动不露，**不要为某个平台单独写判断**。物件/手拭巾不带 sku（攒的不卖）。
- 判平台只认 `Store.canBuy()` / `Store.enforce()` / `Account.IS_OVERSEAS` / `RW.internal` 这四个，别再看 UA 或 protocol。
- iOS 攒需求一起出版本；国内安卓可以单独补发，但必须用 main 上已经合进去的提交，不另开分支。
